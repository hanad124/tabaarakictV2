/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "drive.google.com",
      "dropbox.com",
      "previews.dropbox.com",
      "localhost",
    ],
  },
};

module.exports = nextConfig;
